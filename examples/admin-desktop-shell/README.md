# Admin Desktop Shell

Minimal example Tauri desktop admin console shell.

This example exists to define the desktop admin shell role for later packaging and onboarding work. It is a desktop admin console and not a web dashboard.
