# TC-05 Migration Record

- Task ID: `TC-05`
- Title: `Create Initial Test Harness and Check Scripts`
- Date: `2026-05-14`

## Scope Completed

- Created test harness directories:
  - `tests/baseline/`
  - `tests/contracts/`
  - `tests/integration/`
  - `tests/ipc/`
  - `tests/regression/`
- Added script harness files:
  - `scripts/check_all.sh`
  - `scripts/baseline.sh`
  - `scripts/tauri_smoke.sh`
- Added smoke test:
  - `tests/baseline/script_smoke.rs`

## Test-First Artifacts

- Existing namespace smoke test retained:
  - `tests/baseline/module_namespace.rs`
- Added script presence smoke test:
  - `tests/baseline/script_smoke.rs`

## Behavior Notes

- `scripts/check_all.sh` is the primary wrapper for `cargo check`, `cargo test`, and `scripts/tauri_smoke.sh`.
- `scripts/tauri_smoke.sh` exits with status `2` and a documented blocked reason when no Tauri shell is present.

## Unresolved Issues

- Runtime verification of scripts/tests is pending user execution.
- `scripts/tauri_smoke.sh` is expected to report `BLOCKED` until a verified `src-tauri/tauri.conf.*` exists.

## Pending User-Run Verification Commands

```bash
cd /home/pc/Downloads/inf/plan/license-control-suite && cargo test
cd /home/pc/Downloads/inf/plan/license-control-suite && bash scripts/check_all.sh
cd /home/pc/Downloads/inf/plan/license-control-suite && bash scripts/tauri_smoke.sh
```
