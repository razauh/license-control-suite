# TC-06 Migration Record

- Task ID: `TC-06`
- Title: `Reconcile Cargo Dependencies`
- Date: `2026-05-14`

## Scope Completed

- Updated unified crate manifest:
  - `Cargo.toml`
- Added dependency decision record:
  - `docs/migration/dependency_decisions.md`
- Added dependency verification script (test-first artifact):
  - `scripts/check_dependencies.sh`

## Test-First Artifact

- `scripts/check_dependencies.sh` includes checks for:
  - `cargo check`
  - `cargo tree -d`
  - `cargo metadata --format-version 1`
  - explicit failure if dependency tree includes external git `shared-contracts`

## Unresolved Issues

- `Cargo.lock` is not yet generated/updated because cargo commands were not run.
- Dependency validation commands are pending user execution.

## Pending User-Run Verification Commands

```bash
cd /home/pc/Downloads/inf/plan/license-control-suite && cargo check
cd /home/pc/Downloads/inf/plan/license-control-suite && cargo tree -d
cd /home/pc/Downloads/inf/plan/license-control-suite && cargo metadata --format-version 1
cd /home/pc/Downloads/inf/plan/license-control-suite && bash scripts/check_dependencies.sh
```
